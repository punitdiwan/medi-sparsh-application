import UnitManager from "@/Components/doctor/settings/pathology/UnitManager";

export const dynamic = "force-dynamic";

const page = () => {
    return (
        <UnitManager />
    );
};

export default page;
