import ParameterManager from "@/Components/doctor/settings/pathology/ParameterManager";

export const dynamic = "force-dynamic";

const page = () => {
    return (
        <ParameterManager />
    );
};

export default page;
