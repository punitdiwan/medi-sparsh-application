import CategoryManager from "@/Components/doctor/settings/pathology/CategoryManager";

export const dynamic = "force-dynamic";

const page = () => {
    return (
        <CategoryManager />
    );
};

export default page;
